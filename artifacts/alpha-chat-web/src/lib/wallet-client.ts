/**
 * wallet-client.ts — placeholder.
 * Re-esporta da wallet-stub per retrocompatibilità durante la transizione.
 * Verrà rimosso quando la nuova integrazione sarà pronta.
 */
export {
  USDA_CONTRACT_ADDRESS,
  USDA_CHAIN_ID,
  USDA_DECIMALS,
  walletModal,
} from "./wallet-stub";

export const WC_PROJECT_ID: string = "";
