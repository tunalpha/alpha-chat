PHASE 4 — BREEZ SPARK CONTROLLED INTEGRATION
KEystore + Fee Admin + BTC Treasury + E2E
ZERO REGRESSION / ZERO GO-LIVE

OBIETTIVO

Ora che Phase 3.1 Regression Audit è conclusa con successo (773/773 test PASS, build production PASS, Spark completamente dormant con feature flag OFF), procedi con la fase successiva di integrazione controllata.

REGOLA ASSOLUTA:

NON modificare il comportamento esistente di:
- BTC on-chain
- BTC fee model esistente
- EVM / MultiChain
- USDA
- Payment Engine
- Signal / E2E
- Chat
- WalletContext BTC esistente
- Treasury BTC esistente, salvo esclusivamente l'aggiunta isolata del supporto contabile Spark prevista sotto.

NON attivare Spark in produzione.
spark_lightning_enabled DEVE rimanere FALSE per tutta questa fase.

NON fare refactoring generale.
NON rinominare componenti esistenti.
NON sostituire architetture già testate.

==================================================
1. LIVE SPARK ADAPTER — KEYSTORE WIRING
==================================================

Risolvere R3:

LiveSparkAdapter._getMnemonic() attualmente è un placeholder.

Collegare il LiveSparkAdapter al keystore reale di Alpha Wallet in modo che:

- il mnemonic/seed venga recuperato esclusivamente lato client;
- il seed NON venga mai inviato al backend;
- il seed NON venga salvato in MongoDB;
- il seed NON venga scritto nei log;
- il seed NON venga passato a console.log/error;
- il seed NON venga inserito in localStorage;
- Spark riceva il seed esclusivamente per la derivazione locale;
- ExternalSigner continui a garantire che le private key non lascino il dispositivo.

IMPORTANTE:

Riutilizzare la seed BIP39 esistente di Alpha Wallet secondo l'architettura già approvata:

BTC:
m/84'/0'/0'/0/n

Spark:
m/8797555'/1'/0'

NON modificare la derivazione BTC.

NON creare una seconda seed se non tecnicamente indispensabile.

Aggiungere test che dimostrino:

1. stesso mnemonic → BTC derivation invariata;
2. stesso mnemonic → Spark identity invariata;
3. BTC key != Spark identity;
4. mnemonic non presente nelle API request;
5. mnemonic non presente nei log;
6. Spark non può accedere alle private key BTC direttamente;
7. WalletContext BTC rimane funzionalmente invariato.

==================================================
2. SPARK FEE ADMIN — SEPARATA DA BTC
==================================================

Implementare la configurazione amministrativa della fee Spark.

REQUISITO FONDAMENTALE:

La fee Spark deve essere COMPLETAMENTE SEPARATA dalla fee BTC on-chain.

NON riutilizzare:
- btc_fee_config
- BTC fee controller
- BTC fee UI
- BTC fee storage

Creare una configurazione specifica:

spark_fee_bps

Default:
0.10% = 10 bps

La modifica della fee Spark deve:

- essere disponibile esclusivamente al super_admin;
- avere una UI Admin separata e chiaramente denominata "Spark / Lightning Fee";
- NON modificare la fee BTC;
- generare audit event SPARK_FEE_UPDATED;
- avere validazione dei valori;
- impedire valori negativi;
- mantenere invariato il BTC fee configuration.

Mostrare nella UI almeno:

Spark / Lightning Fee
Current: 0.10%

e consentire la modifica.

NON attivare comunque Spark.

==================================================
3. FEE ENGINE
==================================================

Verificare il modello:

Alpha Platform Fee:
0.10%

Breez / Spark / Lightning provider fee:
separata

La UI futura deve mostrare chiaramente:

Recipient receives
Alpha fee
Spark / Lightning network fee
Total paid

Non confondere mai:

Alpha fee ≠ Breez routing fee ≠ BTC network fee.

Recipient-exact deve essere mantenuto dove tecnicamente garantibile.

Non inventare fee Breez.
Se la fee reale non è disponibile, rappresentarla come provider fee determinata dall'SDK.

==================================================
4. BTC TREASURY
==================================================

Preparare l'integrazione contabile della fee Spark con lo STESSO BTC Treasury già utilizzato da Alpha.

REQUISITO:

NON creare un nuovo Treasury BTC.

La fee Alpha derivante da Spark/Lightning deve appartenere allo stesso BTC Treasury esistente.

Tuttavia:

NON implementare ancora sweep automatici reali on-chain se questo richiede modifiche rischiose al Treasury esistente.

Prima implementare esclusivamente:

- ledger/accounting della fee Spark;
- identificazione della source = spark_lightning;
- distinzione transaction type;
- audit trail;
- contabilizzazione separata;
- mapping verso lo stesso BTC Treasury.

Aggiungere guardrail per impedire che una fee Spark venga accidentalmente contabilizzata come fee BTC on-chain.

Se il Treasury esistente richiede modifiche strutturali, fermarsi prima della modifica e produrre un report.

==================================================
5. SEND / RECEIVE LIVE
==================================================

Dopo il wiring del keystore, preparare test reali controllati:

A. connect()
B. getInfo()
C. syncWallet()
D. receive BOLT11
E. receive Spark address
F. send Spark-to-Spark
G. send BOLT11
H. listPayments()

NON utilizzare fondi reali significativi.

Se un test richiede fondi reali, fermarsi e indicare esattamente cosa serve.

NON eseguire automaticamente transazioni reali senza un'azione esplicita di conferma.

==================================================
6. IDB SECURITY
==================================================

Il precedente audit ha rilevato:

payments store → JSON in chiaro
HTLC preimage leggibile.

Prima del go-live:

- documentare esattamente quali dati Breez salva;
- verificare se il SDK offre un meccanismo ufficiale di cifratura/custom storage;
- NON implementare una cifratura custom improvvisata senza verificare compatibilità con il SDK.

Se non esiste soluzione supportata dal SDK:

NON alterare arbitrariamente lo storage.

Produrre invece:
- rischio;
- dati esposti;
- mitigazioni possibili;
- raccomandazione;
- decisione richiesta.

==================================================
7. iOS
==================================================

Non considerare risolto il problema iOS solo tramite simulazione.

Preparare una checklist E2E per dispositivo fisico:

- iPhone Safari/PWA
- connect
- sync
- receive
- background
- foreground resume
- WebSocket
- webhook
- push notification
- recovery

NON dichiarare iOS GO finché il test fisico non viene eseguito.

==================================================
8. FEATURE FLAG — OBBLIGATORIO
==================================================

Durante tutta la fase:

spark_lightning_enabled = false

Con flag FALSE:

- nessun SDK Spark caricato;
- nessun connect;
- nessuna chiamata Spark;
- nessun wallet Spark inizializzato;
- nessuna modifica al portfolio esistente;
- nessun cambiamento alla fee BTC;
- nessuna modifica al comportamento utente.

Aggiungere/aggiornare test E2E che lo dimostrino.

==================================================
9. REGRESSION TEST OBBLIGATORIO
==================================================

Prima di dichiarare completata la fase:

Frontend:
773+ test oppure tutti i test disponibili → PASS

Backend:
unit test → PASS

TypeScript:
0 nuovi errori

Build production:
PASS

E2E con Spark OFF:
- zero Spark requests
- zero Spark modules
- zero Spark IDB
- zero Spark WebSocket
- nessun cambiamento al portfolio
- nessuna regressione BTC/EVM/USDA/Signal/Chat

Aggiungere test specifici:

spark-keystore-isolation.test.ts
spark-fee-isolation.test.ts
spark-treasury-isolation.test.ts
spark-feature-flag.test.ts
spark-security.test.ts

==================================================
10. STOP CONDITIONS
==================================================

FERMATI IMMEDIATAMENTE e NON procedere oltre se:

- il keystore richiede modifica al WalletContext BTC;
- il seed rischia di uscire dal client;
- Spark modifica la derivazione BTC;
- Spark modifica la fee BTC;
- Spark modifica il Treasury BTC esistente in modo non isolato;
- Spark viene caricato con feature flag OFF;
- vengono introdotte regressioni;
- è necessario modificare Chat/Signal/MultiChain/USDA;
- è necessario modificare il Payment Engine esistente;
- è necessario disattivare test esistenti per far passare la build.

In questi casi NON aggirare il problema.
Produci un report e fermati.

==================================================
11. NESSUN GO-LIVE
==================================================

NON:

- impostare spark_lightning_enabled=true;
- mostrare Spark agli utenti;
- effettuare deploy production con Spark attivo;
- eseguire sweep reali verso Treasury;
- dichiarare production-ready.

Questa fase serve esclusivamente a completare l'integrazione controllata e preparare il sistema.

==================================================
12. REPORT FINALE
==================================================

Al termine restituisci:

1. File modificati
2. File NON modificati
3. Keystore wiring risultato
4. Fee Admin risultato
5. Treasury integration risultato
6. Send/Receive test risultati
7. IDB security risultato
8. iOS test stato
9. Feature flag isolation risultato
10. Tutti i test e relativo numero PASS/FAIL
11. Eventuali regressioni trovate e corrette
12. Eventuali rischi residui
13. Eventuali STOP CONDITION
14. Stato finale:

GREEN = pronto per fase finale di go-live review
YELLOW = integrazione completata ma con condizioni aperte
RED = problema architetturale o regressione

RICORDA:

La priorità assoluta è preservare Alpha Wallet esattamente come funziona oggi.

Spark è un modulo aggiuntivo isolato.
BTC rimane invariato.
BTC fee rimane invariata.
BTC Treasury rimane lo stesso Treasury.
Spark fee è separata.
Spark rimane OFF fino all'approvazione finale.