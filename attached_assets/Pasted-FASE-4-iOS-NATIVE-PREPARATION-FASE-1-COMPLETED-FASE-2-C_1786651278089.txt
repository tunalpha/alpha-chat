FASE 4 — iOS NATIVE PREPARATION

FASE 1 ✅ COMPLETED
FASE 2 ✅ COMPLETED
FASE 3 ✅ COMPLETED

Capacitor 8.5.0 is installed.
The iOS project has been generated successfully.

Now prepare the iOS project for the FIRST REAL iPhone BUILD.

IMPORTANT:
This phase must remain strictly focused on iOS project preparation.

DO NOT modify application functionality.

DO NOT modify:
- Spark
- Lightning
- Breez
- Alpha Wallet logic
- BTC
- EVM
- USDA
- Signal/E2E
- WalletConnect
- Face ID/WebAuthn implementation
- Push
- WebRTC
- application routing
- authentication
- backend APIs

DO NOT add native plugins yet.

DO NOT implement:
- APNs
- PushKit
- CallKit
- native biometric plugin
- native Spark
- native Lightning
- WalletConnect native deep links
- Lightning native deep links

TASKS

1. Audit the generated iOS project.

2. Verify:
- App.xcodeproj
- Bundle Identifier
- Info.plist
- AppDelegate.swift
- Capacitor configuration
- web assets
- Assets.xcassets
- LaunchScreen
- deployment target
- Swift Package Manager configuration

3. Prepare the provisional Bundle ID:

com.alphachat.app

Do not create certificates or provisioning profiles.

4. Identify the permissions that will eventually be required for:
- camera
- microphone
- Face ID

BUT DO NOT add unnecessary permissions yet unless they are required for the FIRST basic build.

5. Verify that the project can be opened correctly by Xcode on macOS.

6. Verify that the locally synchronized web assets are correctly located inside:

ios/App/App/public/

7. Verify that Capacitor is configured to load local bundled assets.

IMPORTANT:
DO NOT set:

server.url = "https://alphachat.sbs"

The first native test must use the locally bundled web assets.

8. Check for any iOS-specific build blockers.

If something requires macOS/Xcode, report it instead of inventing a workaround on Linux/Replit.

9. DO NOT modify the Web/PWA.

10. Run the existing frontend tests and production build if possible.

11. Verify that the iOS project generated by Capacitor 8.5.0 is internally consistent and ready to be opened in Xcode.

IMPORTANT SAFETY RULE:

If you discover a problem requiring changes outside this phase, STOP and report it.

Do not solve unrelated problems automatically.

FINAL REPORT

Provide:

A. iOS project status
B. Bundle ID status
C. Xcode compatibility status
D. Swift Package Manager status
E. Info.plist status
F. Assets status
G. Web asset status
H. Permissions currently present and permissions that will eventually be needed
I. Any blockers requiring macOS/Xcode
J. Frontend tests
K. Production build
L. Any files modified
M. Explicit confirmation that the following were NOT modified:

- Spark/Lightning
- Breez
- Alpha Wallet
- BTC
- EVM
- USDA
- Signal/E2E
- WalletConnect
- Face ID/WebAuthn
- Push
- WebRTC
- routing
- authentication
- backend APIs
- Web/PWA behavior

STOP after this report.

DO NOT proceed to native plugins.
DO NOT implement native functionality.
DO NOT proceed to Phase 5.

Wait for approval before making any further changes.